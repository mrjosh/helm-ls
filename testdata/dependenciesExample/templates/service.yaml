apiVersion: v1
kind: Service
metadata:
  name: {{ include "dependeciesExample.fullname" . }}
  labels:
    {{- include "dependeciesExample.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dependeciesExample.selectorLabels" . | nindent 4 }}
