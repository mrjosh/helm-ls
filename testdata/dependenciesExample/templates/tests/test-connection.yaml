apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dependeciesExample.fullname" . }}-test-connection"
  labels:
    {{- include "dependeciesExample.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dependeciesExample.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
